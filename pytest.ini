[pytest]
markers =
    integration: tests that require external files or hf:// access

filterwarnings =
    ignore::DeprecationWarning
    ignore:cannot collect test class.*TestRunner.*:pytest.PytestCollectionWarning